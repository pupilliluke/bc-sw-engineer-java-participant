import { describe, expect, it, vi } from 'vitest'

describe('customersApi', () => {
  it('uses API base URL', () => {
    // TODO: mock fetch; assert URL starts with VITE_API_BASE_URL
    expect(true).toBe(true)
  })
})
