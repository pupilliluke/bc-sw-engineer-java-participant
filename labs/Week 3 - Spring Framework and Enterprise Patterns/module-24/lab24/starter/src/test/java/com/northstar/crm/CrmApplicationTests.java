package com.northstar.crm;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.fail;

/**
 * Timed path: replace this stub with a real {@code @SpringBootTest} after
 * WebServiceConfig + endpoint TODOs (see LAB-24-GUIDE Step 8).
 */
class CrmApplicationTests {
  @Test
  void contextLoadsAndRestSeedVisible() {
    fail("TODO: @SpringBootTest + assert seeded CUS-1001 via CustomerService");
  }
}
