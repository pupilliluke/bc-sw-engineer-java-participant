# Example inventory at lab root (sibling of infra/). Never commit production IPs or secrets.
all:
  children:
    crm:
      hosts:
        crm-dev-01:
          ansible_host: 127.0.0.1
          # TODO(lab45): ansible_user, become method — syntax-check only unless instructor authorizes a host
      vars:
        environment: dev
