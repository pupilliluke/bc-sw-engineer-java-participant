# Triage CSV sketch

Work in **`java-bootcamp/examples/module-40-exercises/notes/`** (not the course clone).

Canonical Lab 40 headers:

```csv
finding_id,source,package_or_location,cve_or_rule,cvss,classification,owner,due_date,notes
lab40-001,dependency-check,TODO:group:artifact:version,CVE-TODO,7.5,needs_review,student,YYYY-MM-DD,TODO transitive path
```

Classifications: `confirmed` · `false_positive` · `mitigated` · `accepted` · `needs_review`
