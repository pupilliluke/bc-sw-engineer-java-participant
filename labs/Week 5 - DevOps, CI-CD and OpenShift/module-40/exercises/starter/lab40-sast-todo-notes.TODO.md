# SAST path TODOs

Work in **`java-bootcamp/examples/module-40-exercises/notes/`**.

Lab 39 read API is `GET /api/customers` (no `{id}` yet; no Spring Security).

- [ ] Request → `CustomerController.list` → repository sink
- [ ] Authz: none on the list API — record as a gap
- [ ] Secrets / unsafe logging
- [ ] Record class#method for each note
