# CI runbook outline

- Workflow path: java-bootcamp/.github/workflows/crm-ci.yml
- When PR vs main runs what
- How to re-run failed verify + local `mvn -B clean verify`
- Where artifacts / checksum live (`crm-jar` on main/tags)
- Secret *names* only

**Self-mark:** Pass / Fail
