# Runbook outline

- Namespace `crm-training` / k3d kubeconfig note (no secrets in Git)
- kubectl apply listed files (never `-f k8s/`)
- Secret created out-of-band
- rollout status
- Host-header smoke on http://127.0.0.1:8088 (`GET /api/customers`)
- rollback undo

**Self-mark:** Pass / Fail
