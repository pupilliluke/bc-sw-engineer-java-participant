apiVersion: v1
kind: Secret
metadata:
  name: crm-api-secrets
  labels:
    app: crm-api
type: Opaque
stringData:
  # TODO(lab42): DOCUMENT keys only — create real Secret out-of-band in the training namespace.
  # NEVER commit real passwords, tokens, or kubeconfig.
  CRM_DB_PASSWORD: "REPLACE-OUT-OF-BAND"
  # CRM_DB_USER: "REPLACE-OUT-OF-BAND"
