# Lab-only teaser. Unrestricted Actuator is not for public prod hosts.
logging:
  level:
    com.northstar.crm: INFO
management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: never
